app:
  message: "Running TEST profile: showing queries, and TRACE logging level"

data-source:
  employee:
    entries-number-to-load: 200

logging:
  level:
    root: "trace"
    org:
      hibernate:
        SQL: "trace"
        type:
          descriptor:
            sql: "trace"

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        